package main

import (
    //"fmt"
    //"strings"
    //"net/http"
)

//httpPost
func httpPost() {

    //func (c *Client) Post(url string, bodyType string, body io.Reader) (resp *Response, err error)
    //resp, err := http.Post(url, bodyType, strings.NewReader("name=cjb"))

    //if err != nil {
    //    fmt.Println(err)
    //}
    
    //defer resp.Body.Close()
    //body, err := ioutil.ReadAll(resp.Body)
    
    //if err != nil {
        // handle error
    //}
 
    //fmt.Println(string(body))
}
